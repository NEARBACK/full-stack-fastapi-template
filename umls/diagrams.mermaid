
flowchart LR
    subgraph Client[Frontend]
        A[React App]
    end

    subgraph Server[Backend]
        B[FastAPI Service]
    end

    subgraph Storage[Database]
        P[(PostgreSQL)]
    end

    A -->|HTTP JSON| B
    B -->|SQL| P
    A <-->|Static assets optional| B

